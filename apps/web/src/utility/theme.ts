import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#854621",
    },
    secondary: {
      main: "#ff6d1f",
    },
    background: {
      default: "#ffffff",
      paper: "#fdfcf7",
    },
    text: { primary: "#383230" },
  },
});

export default theme;
